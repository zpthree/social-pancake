type Query {
  hello(name: String): String!
}

type Mutation {
  createUser(
    name: String!
    email: String!
    username: String!
    password: String!
  ): User!
}

type User {
  id: ID!
  name: String!
  email: String!
  username: String!
}
